import 'package:get/get.dart';

class ModeSelectionController extends GetxController {
  // We don't need mode selection methods anymore since we merged the functionality
  // into a single screen with categories
}

enum QuizMode {
  practice, // We'll keep only practice mode since we merged the functionality
}
